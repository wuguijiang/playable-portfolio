# 项目清理报告

执行时间：2026-09-16
状态：**第 1 / 2 / 3 档已全部执行完毕，构建验证通过**

---

## 一、结果总览

| 指标 | 清理前 | 清理后 |
|---|---|---|
| git 跟踪文件数 | 1004 个 | **97 个** |
| 根目录散落文件 | devserver.log、devserver-error.log、lianliankan-screenshot.png | 已清空 |
| `src/` 死代码与无引用素材 | Works.vue + 1.4MB 无引用图 + 6 张孤儿图 | 已移除 |
| `public/cocos-js/` | 5.5 MB | 已移除（**已实测确认可删**） |
| `dist/` | 93 MB（过期构建产物） | 已移除 |
| 构建验证 | — | ✅ `vite build` 443ms 通过，零错误 |

---

## 二、已执行的清理项

### 第 1 档 · 零风险垃圾

| 路径 | 体积 | 原因 |
|---|---|---|
| `devserver.log` | 1 KB | dev server 输出残留 |
| `devserver-error.log` | 0 KB | 空文件 |
| `lianliankan-screenshot.png` | 3 KB | 全项目无引用 |
| `src/views/Works.vue` | 4.5 KB | 死代码：`/works` 已 redirect 到 `/tools`，无人 import |
| `src/assets/method-flow.png` | 1.4 MB | 全项目无引用 |
| `src/assets/tools/assistant-06-menu-ext.png` | 24 KB | Tools.vue 未引用 |
| `src/assets/tools/assistant-07-mgr-three.png` | 44 KB | Tools.vue 未引用 |
| `src/assets/tools/assistant-08-open-mcp.jpg` | 88 KB | Tools.vue 未引用 |
| `src/assets/tools/assistant-09-mcp-running.png` | 272 KB | Tools.vue 未引用 |
| `src/assets/tools/cocos-mcp-assistant.png` | 152 KB | Tools.vue 未引用 |
| `src/assets/tools/playable-sandbox.png` | 1.6 MB | Tools.vue 未引用 |

### 第 3 档 · 实测确认后移除

| 路径 | 体积 | 实测结论 |
|---|---|---|
| `public/cocos-js/` | 5.5 MB | **确认是打包中间产物，可安全删除** |

验证方法（对照实验）：

1. 把 `lianliankan.html` 放到一个**没有** `cocos-js` 的目录，用静态服务器跑起来
2. 同时搭一组**有** `cocos-js` 的对照组，目录结构与真实项目一致
3. 用 Edge 无头模式分别加载两组，比对结果：

| 对比项 | 实验组（无 cocos-js） | 对照组（有 cocos-js） |
|---|---|---|
| 截图 MD5 | `811d42087039...` | `811d42087039...` **完全相同** |
| DOM 大小 | 4,940,127 字节 | 4,940,127 字节 **完全一致** |
| 服务器请求日志 | 只有 html + favicon | 只有 html + favicon |

**两组服务器日志里都没有任何 `/cocos-js/` 请求** —— 连文件就在旁边、随时可取的情况下，浏览器也压根没去取。

说明 5 个试玩 HTML 都是**完整的自包含单文件包**，各自把用到的引擎内嵌进去了：

- `lianliankan.html` / `xiangqi.html` → 内嵌旧版 `cocos-js/cc.js`（2,391,952 B）
- `daxigua.html` → 内嵌新版 `cocos-js/cc.a2f60.js`（2,526,874 B）
- `beach-toast.html` / `rabbit.html` → 以内嵌 JSON 键值形式内嵌

### 第 2 档 · 移出版本控制

| 项目 | 处理 |
|---|---|
| `node_modules/`（833 个文件） | 已 `git rm -r --cached` 移出索引，**本地文件完整保留** |
| `dist/`（74 个文件） | 已移出索引，并已移动到备份目录 |

---

## 三、备份在哪、怎么还原

所有被移除的内容都**没有真正删除**，完整移动到了：

```
C:\Users\lsj\Desktop\个人试玩广告简历\_cleanup_backup_2026-09-16\
```

总大小 102 MB，原目录结构完整保留：

```
_cleanup_backup_2026-09-16/
├── devserver.log
├── devserver-error.log
├── lianliankan-screenshot.png
├── dist/                      ← 过期构建产物（可 npm run build 重建）
├── public/cocos-js/           ← 打包中间产物
├── src/views/Works.vue
├── src/assets/method-flow.png
├── src/assets/tools/          ← 6 张孤儿图
└── 旧版-项目清理清单.md
```

- **想还原**：把对应文件移回原位即可
- **确认不需要**：直接删掉这个文件夹，可释放 102 MB

该目录已写入 `.gitignore`，不会被 git 跟踪。

---

## 四、本次新增的文件

| 文件 | 作用 |
|---|---|
| `.gitignore` | **新增**。忽略 `node_modules/`、`dist/`、`*.log`、`_cleanup_backup_*/`、`.workbuddy/` 等，防止构建产物再次进入版本库 |
| `项目清理报告.md` | 本文件 |

---

## 五、剩余待办（需要你决定）

### 1. git 提交还没做

`git rm --cached` 已经把 927 项移除记录放进暂存区，但**没有提交**。当前 `git status` 会显示这批 staged deletion。确认没问题后执行：

```bash
git commit -m "chore: 添加 .gitignore，移除 node_modules 与 dist 的版本跟踪"
```

### 2. `.git` 仍有 117 MB（历史瘦身）

`.gitignore` 只能防止**未来**再膨胀，管不了已经写进历史的大文件。当前历史里还躺着：

| 历史中的文件 | 大小 |
|---|---|
| `dist/playable/rabbit.html` | 38.2 MB |
| `node_modules/@rolldown/binding-win32-x64-msvc/*.node` | 19.6 MB |
| `dist/playable/lianliankan.html` | 11.9 MB |
| `dist/playable/daxigua.html` | 6.3 MB |

要清掉需要**改写 git 历史**（`git filter-repo`），属于高风险操作：会改变所有 commit 的 hash。建议单独确认、并且先整仓备份再动手。

### 3. 体积优化（不影响功能，收益可观）

| 目标 | 现状 | 建议 |
|---|---|---|
| `public/playable/rabbit.html` | **40 MB**（其余试玩才 3.2–5.5 MB，它一个占 public 的 62%） | 重新打包压缩，收益最大 |
| `public/shi-logo.png` + `src/assets/shi-logo.png` | 两张 **MD5 完全相同**的图，各 2.1 MB | 合并去重 + 压到 50 KB 以内（它现在被当 favicon 用） |
| `src/assets/covers/` | 20 MB（11 张封面全部在用，无冗余） | 按实际显示尺寸压图 |
| `src/views/Tools.vue` | 单文件 126 KB | 可用，但后续可考虑拆分 |

---

## 六、清理后的项目结构

```
个人试玩广告简历/
├── .gitignore                    ← 新增
├── .workbuddy/                   ← 本地工具数据（已忽略）
├── _cleanup_backup_2026-09-16/   ← 备份，确认后可删（已忽略）
├── index.html
├── package.json
├── package-lock.json
├── vite.config.js
├── 项目清理报告.md
├── 启动网页.bat
├── node_modules/                 ← 已移出版本控制，本地保留
├── public/
│   ├── playable/                 ← 5 个试玩单文件
│   └── shi-logo.png
└── src/
    ├── App.vue
    ├── main.js
    ├── styles.css
    ├── assets/{covers, tools, shi-logo.png}
    ├── data/works.js
    ├── router/index.js
    └── views/{Home, Play, Tools}.vue
```
